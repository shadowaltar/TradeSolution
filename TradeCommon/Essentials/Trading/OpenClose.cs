namespace TradeCommon.Essentials.Trading;

/// <summary>
/// Tells if the action is to open or to close an order, a position, etc.
/// </summary>
public enum OpenClose
{
    DoNothing,
    Open,
    Close,
}
